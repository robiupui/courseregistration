<%= form_for(@textbook) do |f| %>
  <% if @textbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@textbook.errors.count, "error") %> prohibited this textbook from being saved:</h2>

      <ul>
      <% @textbook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :isbn %><br />
    <%= f.text_field :isbn %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.collection_select :course_id, Course.all.collect, :id, :full_title, :prompt => "Select a course" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
